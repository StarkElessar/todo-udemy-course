import React from "react"
import './SearchPanel.scss'

const SearchPanel = () => {
  return (
    <input className="search-panel" type="text" placeholder="type to search" />
  )
}

export default SearchPanel